describe("'primo-explore-help-menu' package smoke tests", function () {
  beforeEach(module("prmSearchBookmarkFilterAfter"));
  var $componentController, $rootScope, $q, ctrl, expectedIds, bindings;

  it("should run a sample passing test", function () {
    expect(true).toBe(true);
  });
});